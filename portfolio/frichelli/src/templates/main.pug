block variables

doctype html
html(lang="ru")
    head
        meta(charset="windows-1251")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")

        title #{title}
        block scripts

    body
        block header
        block content